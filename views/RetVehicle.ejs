<!-- 입고QC -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회수차량 조회</title>
    <style>
        body, html {
            height: 100%;
            margin: 0; 
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .navbar {
            background-color: white;
            color: white;
            padding: 20px 0;
            text-align: center;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }

        .navbar .logo {
            width: 200px;
            height: auto;
        }

        .data-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 20px;
        }

        .grid-container {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            grid-gap: 1px;
        }

        .grid-header,
        .grid-row {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            align-items: center;
        }

        .grid-header div,
        .grid-row div {
            border : 1px solid #ccc;
            padding: 10px;
            text-align: center;
            min-width: 120px;
        }

        .grid-header {
            background-color: white;
            font-weight: bold;
        }

        .grid-row:nth-child(even) {
            background-color: #f2f2f2;
        }

        .grid-row:first-child {
            background-color: #1E90FF;
        }
    
        /*반응형 ui*/
        @media only screen and (max-width: 600px) {
            /* 모바일 */
            .icon-container{
                flex-direction:column;
                align-items:center;
            }

            .icon-box {
                width: 300px;
                height: 150px;
                margin: 10px 0;
            }
    
            .icon-text {
                font-size: 20px;
            }

            .grid-header div,
            .grid-row div {
                padding: 5px;
                min-width: 50px;
                font-size: 7px;
            }

            .grid-header {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <!-- navbar -->
    <div class="navbar">
        <a href="/main"><img class="logo" src="https://pds.saramin.co.kr/company/logo/202203/08/r8etng_ipfy-1p4vv0n_logo.png" alt="하모니로고"/></a>
    </div>

<!-- 데이터 출력 -->
<div class="data-container">
    <% if (data && data.length) { %>
        <!-- 스키마 내용 출력 -->
        <div class="grid-row">
            <div>차량번호</div>
            <div>모델명</div>
            <div>고객명</div>
            <div>신차구분</div>
            <div>회수사유</div>
            <div>요청자</div>
            <div>렌탈방식</div>
        </div>
        <!-- 실제 데이터 출력 -->
        <% data.forEach(item => { %>
            <div class="grid-row">
                <div><%= item.carNo %></div>
                <div><%= item.modelName %></div>
                <div><%= item.CliName %></div>
                <div><%= item.GUBUN %></div>
                <div><%= item.RetReason %></div>
                <div><%= item.requester %></div>
                <div><%= item.RentalMethod %></div>
            </div>
        <% }) %>
    <% } else { %>
        <p>존재하는 반납차량 데이터가 없습니다.</p>
    <% } %>
</div>

    <!-- <script src="scripts.js"></script> -->
</body>
</html>
